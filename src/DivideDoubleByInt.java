public class DivideDoubleByInt {
    public static void main(String [] args){
        int x = 10;
        double y = 20.5;

        double sum = y / x;

        System.out.println("The sum of y divided by x is " + sum);
        // had to change the y int variable into a double when changed to a decimal, same with the sum because double is higher than int
    }
}
